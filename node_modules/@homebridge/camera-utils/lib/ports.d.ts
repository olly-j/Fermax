/// <reference types="node" />
import { Socket } from 'dgram';
export declare function releasePorts(ports: number[]): void;
export declare function reservePorts({ count, attemptNumber, }?: {
    count?: number;
    attemptNumber?: number;
}): Promise<number[]>;
export declare function bindToPort(socket: Socket): Promise<number>;
